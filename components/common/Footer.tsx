import { JSX } from "preact";

export function Footer(props: JSX.HTMLAttributes<HTMLElement>) {
  return (
    <footer class="flex items-center justify-center h-[75px] bg-gray-800 text-white text-md">
      Footer da aplicação
    </footer>
  );
}
